dependencies {
	compile project(":modules:adj-admin:adj-admin-api")

	compileOnly group: "com.liferay.portal", name: "release.dxp.api"
}

buildService {
	apiDir = "../adj-admin-api/src/main/java"
}

group = "adj.admin"